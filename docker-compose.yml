version: "3.9"

services:
  api:
    build: .
    container_name: api-go
    ports:
      - "8080:8080"
    restart: always
    environment:
      - PORT=8080
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=youruser
      - DB_PASSWORD=yourpassword
      - DB_NAME=yourdb